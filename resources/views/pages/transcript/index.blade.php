@extends('layouts.app', ['breadcrumbs' => [['href' => route('dashboard'), 'text' => 'Dashboard'], ['text' => 'Transcripts', 'active']]])
@section('title', 'Transcripts')
@section('page_heading', 'Transcripts')
@section('content')
<div class="space-y-6">@if ($errors->has('transcript'))<div class="rounded-lg border border-destructive/40 bg-destructive/10 px-4 py-3 text-sm text-destructive">{{ $errors->first('transcript') }}</div>@endif
@can('create', App\Models\TranscriptSnapshot::class)<april:card><slot:title>Issue a transcript</slot:title><slot:description>Creates an immutable lifetime academic record from the latest official subject results.</slot:description><slot:content><form method="POST" action="{{ route('transcripts.store') }}" class="flex flex-col gap-3 sm:flex-row">@csrf<select name="student_record_id" required class="min-w-0 flex-1 rounded-md border border-input bg-background px-3 py-2 text-sm"><option value="">Choose learner</option>@foreach ($students as $student)<option value="{{ $student->id }}">{{ $student->user?->name ?? $student->admission_number }} · {{ $student->admission_number }}</option>@endforeach</select><input name="reason" class="min-w-0 flex-1 rounded-md border border-input bg-background px-3 py-2 text-sm" placeholder="Reason for revision (optional)"><april:button type="submit">Issue transcript</april:button></form></slot:content></april:card>@endcan
<april:card><slot:title>Issued transcripts</slot:title><slot:content>@if ($transcripts->isEmpty())<p class="py-6 text-sm text-muted-foreground">No transcripts have been issued yet.</p>@else<div class="overflow-x-auto"><table class="w-full text-sm"><thead class="border-b text-left text-muted-foreground"><tr><th class="px-3 py-2">Learner</th><th class="px-3 py-2">Revision</th><th class="px-3 py-2">Issued</th></tr></thead><tbody class="divide-y">@foreach ($transcripts as $transcript)<tr><td class="px-3 py-3">{{ $transcript->studentRecord->user?->name ?? $transcript->studentRecord->admission_number }}</td><td class="px-3 py-3">{{ $transcript->revision }}</td><td class="px-3 py-3">{{ $transcript->issued_at->format('j M Y') }}</td></tr>@endforeach</tbody></table></div>@endif</slot:content></april:card></div>
@endsection
